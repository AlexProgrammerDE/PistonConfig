package net.pistonmaster.pistonconfig.json;

/// Core metadata keys used by the JSON, JSONC, and JSON5 backend.
public final class JsonMetadataKeys {
  /// Metadata key storing the original numeric radix reported by json5-java.
  public static final String NUMBER_RADIX = "json5.number.radix";

  private JsonMetadataKeys() {
  }
}

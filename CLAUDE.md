# Claude Notes

PistonConfig is a Java 25 multi-module Gradle library. Keep the public API format-agnostic and strongly typed. Backends should translate library-specific details into core-owned comments, decorations, source locations, styles, and metadata keys rather than leaking parser types.

Run `./gradlew test` before handing off code changes.
